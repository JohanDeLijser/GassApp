<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="nl.gassapp.gassapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewModel.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ViewModel.GraphActivity" />
        <activity android:name=".ViewModel.AddRefuelActivity" />
        <activity android:name=".ViewModel.ImageAndLocationActivity" />
        <activity android:name=".ViewModel.EditRefuelActivity"></activity>
    </application>

</manifest>